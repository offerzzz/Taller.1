#include <stdio.h>
int main (){
    char materias [3][20];
    float notas [3][5];
    for (int i=0; i<3; i++){
        scanf("%s", materias[i]);
        scanf("%f", &notas[i][j]); 
    }

    return 0;
}